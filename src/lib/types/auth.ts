export interface AuthenticationResult {
  address: string;
  publicKey: string;
  privateKey: Uint8Array;
}

export interface RegistrationResult {
  address: string;
  publicKey: string;
  privateKey: Uint8Array;
}