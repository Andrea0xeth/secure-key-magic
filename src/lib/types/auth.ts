export interface AuthenticationResult {
  address: string;
  publicKey: string;
  mnemonic: string;
}

export interface RegistrationResult {
  address: string;
  publicKey: string;
  mnemonic: string;
}